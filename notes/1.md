```mermaid
flowchart TD
Nodes --> N[really small programs for a highly specific tasks]

Topics --> T["used to send frequent messages of one defined type, such as sensor readouts" 
 >one node can not publish and subscribe to it at the same time, 
 >but there are no restrictions on the number of different nodes publishing or subscribing.]

 Services--> S["one node asks for information to the other."
 >Technically two way comm. cause apart from response, 
 request can contain some info as well
 ]

 Commands--> R1[ros2 run -- used to run one file, without specifying its full path]
             R2[ros2 launch -- used to run multiple nodes , labels in one configuration file]